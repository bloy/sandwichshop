<% content_for :title, "My Profile" %>
<dl>
  <dt>Name</dt>
  <dd><%= @user.full_name %></dd>

  <dt>Last Login</dt>
  <dd>
  <%= l(@user.last_login_at, :format => :short) if @user.last_login_at %>
  </dd>
</dl>

<%= link_to "Edit Profile", edit_user_path, :class => 'action' %>
